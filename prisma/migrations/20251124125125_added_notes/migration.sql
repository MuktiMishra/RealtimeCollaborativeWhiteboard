BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Room] ADD [notes] NVARCHAR(1000) NOT NULL CONSTRAINT [Room_notes_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
